name: wx_lints
description: A collection of custom analyzer-plugin lint rules for Dart and Flutter projects.
version: 2.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.0

dependencies:
  analysis_server_plugin: ^0.3.0
  analyzer: ^14.0.0

dev_dependencies:
  analyzer_testing: ^0.4.2
  test: ^1.25.0
  test_reflective_loader: ^0.2.0
